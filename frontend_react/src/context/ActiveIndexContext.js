import { createContext } from "react";

export const ActiveIndexContext = createContext(0);
export const ActiveIndexDispatchContext = createContext(0);

// export const SelectedColor = createContext("");
// export const SelectedMusicGenre = createContext("");
